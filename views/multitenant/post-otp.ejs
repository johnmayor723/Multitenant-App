<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>EasyApps || Verify OTP</title>
  <link rel="icon" type="image/png" href="/multitenant/img/easyicon.png">
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    :root{
      --left-bg: #e9ecef;
      --right-bg: #ffffff;
    }
    html, body { height: 100%; margin: 0; }
    .row-full { min-height: 100vh; }
    .g-0 { --bs-gutter-x: 0; --bs-gutter-y: 0; }
    .col-left { background: var(--left-bg); }
    .col-right { background: var(--right-bg); }
    .col-left, .col-right {
      display: flex; align-items: center; justify-content: center; padding: 2rem;
    }
    .signup-box {
      width: 100%; max-width: 420px; background: #ffffff; border-radius: 12px;
      padding: 28px; box-shadow: 0 8px 24px rgba(0,0,0,0.06); text-align: center;
    }
    .signup-logo { font-size: 1.6rem; font-weight: 700; margin-bottom: 1.25rem; }
    .signup-heading { margin-bottom: 100px; }
    .btn-signup { margin-bottom: 12px; width: 100%; height: 44px; border-radius: 8px; font-weight: 600; }
    .btn-apple { background:#000; color:#fff; border: none; }
    .btn-google { background: #fff; border: 1px solid #cfcfcf; color: #222; }
    .form-otp { margin-bottom: 18px; }
    .signup-image { max-width: 100%; max-height: 85vh; object-fit: contain; display: block; }
    @media (max-width: 767.98px) {
      .col-left { padding: 1rem; height: 100%; background-color: #fff; }
      .signup-box { padding: 16px; margin-top: 8px; margin-bottom: 8px; box-shadow: none; }
      .signup-logo { font-size: 1.25rem; margin-bottom: 0.75rem; }
      .signup-heading { margin-bottom: 20px; }
      .signup-image { max-height: 50vh; }
      .col-right { display: none; }
    }
    .input-group .form-control {
      height: 44px; border-radius: 8px;
    }
    .btn-submit {
      height: 44px; border-radius: 8px; font-weight: 600; width: 100%;
    }
  </style>
</head>
<body>

  <div class="row g-0 row-full">
    <!-- RIGHT image -->
    <div class="col-12 col-md-6 col-right">
      <img class="signup-image" src="/multitenant/img/easylogo.png" alt="Verify OTP illustration" 
           onerror="this.onerror=null;this.src='https://via.placeholder.com/800x600?text=Verify+OTP'">
    </div>

    <!-- LEFT form -->
    <div class="col-12 col-md-6 col-left">
      <div class="signup-box" aria-labelledby="verify-heading">

        <!-- Mobile logo -->
        <div class="mobile-header d-md-none mb-3">
          <img class="signup-image" height="40px" src="/multitenant/img/easylogo.png" alt="EasyApps Logo" 
               onerror="this.onerror=null;this.src='https://via.placeholder.com/150x50?text=EasyApps'">
        </div>

        <!-- Heading -->
        <div style="margin-top: 80px;" id="verify-heading" class="signup-logo signup-heading">
          🔑 Verify Your Email
        </div>

        <!-- OTP verification form -->
       <form class="form-otp" action="/multitenant/verify-otp" method="POST" novalidate>
          <!-- Hidden email field -->
          <input 
            type="hidden" 
            name="email" 
            value="<%= email %>" 
          />

          <!-- OTP field -->
          <input
            type="text"
            name="otp"
            class="form-control mb-3"
            placeholder="Enter OTP"
            maxlength="6"
            required
          />

          <button class="btn btn-dark btn-submit" type="submit">Verify OTP</button>
        </form>


        <!-- Resend link -->
        <div class="text-muted small mb-3">
          Didn’t receive the code? <a href="/tenant-auth/request-otp" class="fw-semibold">Resend OTP</a>
        </div>

        <div style="margin-top:12px; font-size:0.95rem;">
          Already have an account? <a href="/login" class="fw-semibold">Sign in</a>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
