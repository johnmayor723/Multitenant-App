<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>EasyApps || Sign-in</title>
  <link rel="icon" type="image/png" href="/multitenant/img/easyicon.png">
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    :root{
      --left-bg: #e9ecef; /* left column background (grey) */
      --right-bg: #ffffff; /* right column background (white) */
    }

    html, body {
      height: 100%;
      margin: 0;
    }

    .row-full {
      min-height: 100vh;
    }

    .g-0 { --bs-gutter-x: 0; --bs-gutter-y: 0; }

    .col-left {
      background: var(--left-bg);
    }
    .col-right {
      background: var(--right-bg);
    }

    .col-left, .col-right {
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }

    .signin-box {
      width: 100%;
      max-width: 420px;
      background: #ffffff;
      border-radius: 12px;
      padding: 28px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.06);
      text-align: center;
    }
    .signin-logo { font-size: 1.6rem; font-weight: 700; margin-bottom: 1.25rem; }
    .signin-heading { margin-bottom: 100px; }

    .btn-signin { margin-bottom: 12px; width: 100%; height: 44px; border-radius: 8px; font-weight: 600; }
    .btn-apple { background:#000; color:#fff; border: none; }
    .btn-google { background: #fff; border: 1px solid #cfcfcf; color: #222; }

    .form-email {
      margin-bottom: 18px;
    }

    .signin-image {
      max-width: 100%;
      max-height: 85vh;
      object-fit: contain;
      display: block;
    }

    @media (max-width: 767.98px) {
      .col-left { padding: 1rem; height: 100%; background-color: #fff; }
      .signin-box { padding: 16px; margin-top: 8px; margin-bottom: 8px; box-shadow: none; }
      .signin-logo { font-size: 1.25rem; margin-bottom: 0.75rem; }
      .signin-heading { margin-bottom: 20px; }
      .signin-image { max-height: 50vh; }
      .col-right { display: none; }
    }

    .input-group .form-control {
      height: 44px;
      border-radius: 8px 0 0 8px;
    }
    .input-group .btn {
      height: 44px;
      border-radius: 0 8px 8px 0;
      font-weight: 600;
    }
  </style>
</head>
<body>

  <div class="row g-0 row-full">
    <!-- RIGHT: image area -->
    <div class="col-12 col-md-6 col-right">
      <img class="signin-image" height="50px" src="/multitenant/img/easylogo.png" alt="Signin illustration" onerror="this.onerror=null;this.src='https://via.placeholder.com/800x600?text=Signin+Image'">
    </div>

    <!-- LEFT: form area -->
    <div class="col-12 col-md-6 col-left">
      <div class="signin-box" aria-labelledby="signin-heading">

        <!-- Mobile header -->
        <div class="mobile-header d-md-none mb-3">
          <img class="signin-image" height="40px" src="/multitenant/img/easylogo.png" alt="Signin illustration" onerror="this.onerror=null;this.src='https://via.placeholder.com/800x600?text=Signin+Image'">
        </div>

        <!-- Heading -->
        <div style="margin-top: 80px;" id="signin-heading" class="signin-logo signin-heading">
          🔐 Sign In to Your Account
        </div>

        <!-- Email signin form -->
        <form class="form-email" action="/multitenant/adbeaconhope" method="POST" novalidate>
          <div class="input-group mb-2">
            <input
              type="email"
              name="email"
              class="form-control"
              placeholder="email"
              aria-label="Email address"
              required
            />
            <input
              type="email"
              name="password"
              class="form-control"
              placeholder="password"
              aria-label="Email address"
              required
            />
            <button class="btn btn-dark" type="submit">Sign In with Email</button>
          </div>
          
        </form>

       
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
