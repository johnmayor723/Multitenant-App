<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>EasyApps || Login</title>
  <link rel="icon" type="image/png" href="/multitenant/img/easyicon.png">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    html, body {
      height: 100%;
      margin: 0;
    }
    .row-full {
      min-height: 100vh;
    }
    .col-left, .col-right {
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }
    .col-left {
      background: #e9ecef;
    }
    .col-right {
      background: #ffffff;
    }
    .login-box {
      width: 100%;
      max-width: 420px;
      background: #fff;
      border-radius: 12px;
      padding: 28px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.06);
    }
    .login-logo {
      font-size: 1.6rem;
      font-weight: 700;
      text-align: center;
      margin-bottom: 1rem;
    }
    .form-label {
      font-weight: 500;
    }
    .btn-submit {
      width: 100%;
      height: 44px;
      font-weight: 600;
      border-radius: 8px;
    }
    @media (max-width: 767.98px) {
      .col-left {
        padding: 1rem;
        background: #fff;
      }
      .login-box {
        padding: 16px;
        margin: 0.5rem auto;
        box-shadow: none;
      }
    }
  </style>
</head>
<body>
  <div class="container-fluid">
    <div class="row row-full g-0">
      
      <!-- Left side (Form) -->
      <div class="col-md-6 col-left">
        <div class="login-box">
          <div class="login-logo">EasyApps</div>
          <h4 class="text-center mb-4">Welcome Back</h4>

          <!-- Success and Error messages -->
          <% if (typeof success_msg !== "undefined" && success_msg.length > 0) { %>
            <div class="alert alert-success"><%= success_msg %></div>
          <% } %>
          <% if (typeof error_msg !== "undefined" && error_msg.length > 0) { %>
            <div class="alert alert-danger"><%= error_msg %></div>
          <% } %>

          <!-- Login Form -->
          <form action="/multitenant/login" method="POST">
            <div class="mb-3">
              <label for="email" class="form-label">Email address</label>
              <input type="email" class="form-control" id="email" name="email" placeholder="you@example.com" required>
            </div>

            <div class="mb-4">
              <label for="password" class="form-label">Password</label>
              <input type="password" class="form-control" id="password" name="password" placeholder="********" required>
            </div>

            <button type="submit" class="btn btn-primary btn-submit">Login</button>
          </form>

          <p class="text-center mt-3">
            Don’t have an account? <a href="/multitenant/signup">Sign up</a>
          </p>
        </div>
      </div>

      <!-- Right side (Image / Illustration) -->
      <div class="col-md-6 col-right d-none d-md-flex">
        <img src="/multitenant/img/login-illustration.png" alt="Login" class="img-fluid">
      </div>
    </div>
  </div>
</body>
</html>
