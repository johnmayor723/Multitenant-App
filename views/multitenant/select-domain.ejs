<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Domain Selection</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: Arial, sans-serif;
    }

    body {
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      background-color: #f8f9fa;
    }

    .container {
      display: flex;
      background: white;
      width: 85%;
      max-width: 950px;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      overflow: hidden;
    }

    /* Left Section */
    .left {
      flex: 1;
      padding: 2rem;
    }

    .left h2 {
      font-size: 1.5rem;
      font-weight: bold;
      margin-bottom: 0.5rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .left p {
      font-size: 0.95rem;
      margin-bottom: 1rem;
      color: #444;
    }

    .link-box {
      background: #f1f3f5;
      padding: 0.6rem;
      border-radius: 6px;
      margin-bottom: 1.2rem;
      font-size: 0.9rem;
    }

    .domain-options {
      margin-bottom: 1.5rem;
    }

    .domain-option {
      display: flex;
      align-items: center;
      margin-bottom: 0.8rem;
      font-size: 0.95rem;
    }

    .domain-option input {
      margin-right: 0.5rem;
    }

    .premium {
      display: inline-block;
      background: #303031;
      color: white;
      font-size: 0.7rem;
      padding: 0.2rem 0.4rem;
      border-radius: 4px;
      margin-left: 0.4rem;
    }

    .buttons {
      display: flex;
      gap: 1rem;
    }

    .btn {
      padding: 0.8rem 1.2rem;
      border: none;
      border-radius: 6px;
      font-size: 0.95rem;
      cursor: pointer;
      transition: background 0.3s ease;
    }

    .btn-primary {
      background-color: #1a202c;
      color: white;
    }

    .btn-primary:hover {
      background-color: #2d3748;
    }

    .btn-secondary {
      background-color: #e2e8f0;
      color: #333;
    }

    /* Right Section */
    .right {
      flex: 1;
      background: linear-gradient(135deg, #1e1e1e, #49494a);
      color: white;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 2rem;
    }

    .right-box {
      text-align: center;
    }

    .domain-arrow {
      font-size: 2rem;
      margin: 1rem 0;
    }

    .domain-preview {
      background: white;
      color: black;
      padding: 0.5rem 1rem;
      border-radius: 8px;
      margin: 0.5rem 0;
      display: inline-block;
    }

    @media (max-width: 768px) {
      .container {
        flex-direction: column;
      }
      .right {
        display: none;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Left Section -->
    <div class="left">
      <h2>🎉 Your store is created. Buy domain?</h2>
      <p>Domain will boost your brand identity.</p>

      <div class="link-box">https://take.app/modigitman</div>

      <div class="domain-options">
        <label class="domain-option">
          <input type="radio" name="domain">
          modigitman.com — USD 18.00 (1 year)
        </label>
        <label class="domain-option">
          <input type="radio" name="domain">
          modigitman.co — USD 45.00 (1 year)
        </label>
        <label class="domain-option">
          <input type="radio" name="domain">
          modigitman.link — USD 6.00 (1 year)
        </label>
        <span class="premium">Premium Plan</span>
      </div>

      <div class="buttons">
        <button class="btn btn-primary">Upgrade & Buy Domain</button>
        <button class="btn btn-secondary">Skip</button>
      </div>
    </div>

    <!-- Right Section -->
    <div class="right">
      <div class="right-box">
        <div class="domain-preview">https://yourstorename.easyapps.com</div>
        <div class="domain-arrow">⬇️</div>
        <div class="domain-preview">https://yourstorename.com</div>
      </div>
    </div>
  </div>
</body>
</html>